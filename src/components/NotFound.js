import React from "react";

const style = {
  marginTop: "170px",
  letterSpacing: "1px",
  textAlign: "center"
}

const NotFound = () => (
  <h4 style={style}>
    Sorry, but the page you have requested was not found.
  </h4>
);

export default NotFound;
